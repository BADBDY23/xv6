//simple test file :))

#include "types.h"
#include "user.h"
#include "stat.h"

/*void printf(int fd, char*s, ...)
{
	write(fd, s, strlen(s));
}
*/
int main(void)
{
 printf(1, "first test ... :))\n");
 exit();
}

